# 微信举报模拟器 - 快速部署指南

---

## 📋 三种部署方式

### 🚀 方式一：GitHub Actions 自动部署（推荐！）
**特点**：每次 `git push` 自动部署，无需手动操作

#### 前置准备：
1. 在服务器上先克隆项目到部署目录（如 `/www/wwwroot/wechat-report`）
2. 服务器上已安装 Node.js、PM2、Git

#### 配置步骤：

1. **在 GitHub 仓库设置 Secrets**：
   - 进入仓库 → Settings → Secrets and variables → Actions
   - 添加以下 Secrets：
     | Secret 名称 | 说明 | 示例 |
     |------------|------|------|
     | `SERVER_HOST` | 服务器地址 | `123.45.67.89` 或 `yourdomain.com` |
     | `SERVER_USER` | 服务器用户名 | `root` |
     | `SSH_PRIVATE_KEY` | SSH 私钥 | 你的 id_rsa 私钥内容 |
     | `SSH_PORT` | SSH 端口（可选，默认22） | `22` |
     | `DEPLOY_PATH` | 部署路径 | `/www/wwwroot/wechat-report` |

2. **生成 SSH 密钥（如果没有）**：
   ```bash
   # 在本地生成密钥对
   ssh-keygen -t rsa -b 4096 -C "github-deploy"
   
   # 公钥添加到服务器
   ssh-copy-id root@yourserver.com
   # 或者手动把 ~/.ssh/id_rsa.pub 内容追加到服务器 ~/.ssh/authorized_keys
   ```

3. **配置服务器环境**：
   ```bash
   # 登录服务器
   ssh root@yourserver.com
   
   # 进入部署目录
   cd /www/wwwroot/wechat-report
   
   # 克隆项目（第一次）
   git clone https://github.com/你的用户名/你的仓库.git .
   
   # 复制并配置 .env
   cp .env.example .env
   nano .env  # 修改数据库等配置
   
   # 安装依赖
   npm ci --production
   
   # 启动 PM2
   pm2 start server.js --name wechat-report
   pm2 save
   pm2 startup
   ```

4. **推送代码，自动部署！**
   ```bash
   git add .
   git commit -m "首次部署"
   git push
   ```
   
   推送后，GitHub Actions 会自动触发部署！

---

### 💻 方式二：一键脚本部署（适合本地操作）

#### Windows 用户：
```cmd
# 打开 cmd，进入项目目录
deploy.bat root@yourserver.com
```

#### Linux/Mac 用户：
```bash
# 给脚本添加执行权限
chmod +x deploy.sh

# 执行部署
./deploy.sh root@yourserver.com

# 或指定部署路径
./deploy.sh root@yourserver.com /www/wwwroot/wechat-report
```

#### 前置准备：
1. 服务器上已克隆项目（或通过其他方式上传代码）
2. 本地有 SSH 访问权限
3. 服务器已安装 Node.js、Git、PM2

---

### 🐳 方式三：Docker 一键部署（最简单！）

**特点**：一条命令搞定，无需配置环境

#### 快速启动：
```bash
# 1. 复制环境变量配置
cp .env.example .env

# 2. 编辑 .env（修改密码等）
nano .env

# 3. 一键启动！
docker-compose up -d
```

#### 常用命令：
```bash
# 查看日志
docker-compose logs -f app

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 更新并重新部署
git pull
docker-compose up -d --build
```

---

## 🔧 首次部署后配置

### 1. 创建 .env 配置文件
```bash
cp .env.example .env
nano .env  # 修改数据库密码、JWT密钥等
```

### 2. 数据库配置
- 如果用宝塔：在宝塔面板创建数据库和用户
- 如果用 Docker：已自动创建，无需操作

### 3. 访问系统
- 主页：`http://yourdomain.com`
- 管理后台：`http://yourdomain.com/admin`

### 4. 首次登录
- 用户名：`admin`
- 密码：`.env` 中 `ADMIN_PASSWORD` 配置的值

---

## 📱 宝塔反向代理配置

如果用 Docker 或端口部署，需要配置 Nginx 反向代理：

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    # 前端静态文件
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # API 接口
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 图片缓存
    location /img/ {
        proxy_pass http://127.0.0.1:3000;
        expires 7d;
    }
}
```

---

## 🛠️ 常见问题

### Q: GitHub Actions 部署失败
A: 检查 Secrets 是否正确配置，SSH 密钥是否有权限

### Q: PM2 服务启动失败
A: 检查 .env 配置是否正确，数据库是否可连接

### Q: Docker 数据库连接失败
A: 确认 `DB_HOST=mysql`（Docker 网络内用服务名）

---

## 📝 维护命令

### PM2 管理
```bash
pm2 status              # 查看状态
pm2 logs wechat-report  # 查看日志
pm2 restart wechat-report  # 重启
pm2 stop wechat-report  # 停止
```

### Git 更新
```bash
cd /www/wwwroot/wechat-report
git pull
npm ci --production
pm2 restart wechat-report
```

### 数据库备份
```bash
# 宝塔面板：数据库 → 备份
# 或手动导出
mysqldump -u用户名 -p 数据库名 > backup.sql
```
