# 微信举报模拟器 - 宝塔部署指南

---

## 📋 项目简介

这是一个**微信举报模拟演示系统**，用于教育和开发测试目的。

### 核心功能

- 用户端：输入目标微信号/手机号、选择举报类型、配置账号数量，并展示仿真举报过程。
- 管理后台：管理员登录、查看统计、批量生成激活码、管理激活码状态。
- 安全机制：激活码验证、次数控制、任务记录追踪。

---

## 🛠️ 技术栈

- 后端：Node.js + Express（>= 18）
- 数据库：MySQL / MariaDB（>= 5.7）
- 前端：原生 HTML/CSS/JS
- 认证：JWT
- 密码：bcryptjs

---

## 🚀 推荐部署流程（宝塔）

### 1. 环境准备

1. 登录宝塔面板。
2. 安装 Node.js 18+。
3. 安装 MySQL 或 MariaDB。
4. 确认服务器可使用 `npm`。

### 2. 创建数据库与账号

1. 宝塔 → 数据库 → 添加数据库。
2. 创建数据库名，例如 `wechat_report`。
3. 创建普通数据库用户，例如 `wechat_user`。
4. 给该用户分配该数据库权限。

> 生产环境请勿使用 root 数据库账号。

### 3. 上传项目文件

1. 宝塔 → 网站 → 添加站点。
   - 域名：填写你的域名
   - 根目录：`/www/wwwroot/你的域名`
   - PHP版本：无需 PHP
2. 上传或 Git 克隆本项目到站点根目录。
3. 确认目录包含：`server.js`, `db.js`, `package.json`, `public/`。

### 4. 创建 `.env`

在项目根目录创建 `.env`：

```env
PORT=3000
DB_HOST=localhost
DB_USER=wechat_user
DB_PASSWORD=你的数据库密码
DB_NAME=wechat_report
JWT_SECRET=随机强字符串
ADMIN_PASSWORD=强管理员密码
DEMO_CODE=DEMO-2026-TEST
DEMO_MAX_USES=50
```

> `JWT_SECRET` 与 `ADMIN_PASSWORD` 必须设置为安全值。

### 5. 安装依赖

使用宝塔终端或 SSH 进入项目目录：

```bash
cd /www/wwwroot/你的域名
npm install
```

### 6. 启动服务

#### 推荐：使用 PM2 管理器

1. 宝塔软件商店 → 安装“PM2 管理器”。
2. 打开 PM2 管理器 → 添加项目。
   - 项目名称：`wechat-report`
   - 启动文件：`/www/wwwroot/你的域名/server.js`
   - 项目目录：`/www/wwwroot/你的域名`
3. 提交并启动。

#### 临时测试

```bash
npm start
```

> 仅用于临时测试，生产环境建议使用 PM2。

---

## 🔧 反向代理是否必须？

### 回答
- 线上部署时，**强烈推荐使用宝塔 Nginx 反向代理**。
- 如果只是本地测试，可直接访问 `http://服务器IP:3000`。
- 生产环境下，反向代理能带来更好的域名访问、HTTPS 支持、隐藏端口、静态资源加速等优势。

### 推荐方式

#### 方案 A：静态资源由 Nginx 提供，API 反向代理到 Node

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name 你的域名;

    root /www/wwwroot/你的域名/public;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /img/ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location ~ /\. {
        deny all;
    }
}
```

#### 方案 B：全部请求直接代理给 Node

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name 你的域名;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

> 方案 A 更适合生产环境，静态资源由 Nginx 提供更稳定。

---

## 🌐 HTTPS 配置

1. 宝塔网站 → SSL → 申请免费证书（Let’s Encrypt）。
2. 启用 HTTPS。
3. 重载 Nginx 配置。

---

## ✅ 访问地址

- 用户端：`https://你的域名/`
- 管理后台：`https://你的域名/admin.html`

默认管理员账号：
- 用户名：`admin`
- 密码：`.env` 中 `ADMIN_PASSWORD`

---

## 📦 数据库初始化

应用首次启动时会自动创建表。也可手动执行：

```bash
npm run init-db
```

---

## 📌 生产建议

- 使用独立数据库账号，不要用 root。
- `.env` 中 `JWT_SECRET` 必须是强随机值。
- `ADMIN_PASSWORD` 不要使用弱密码。
- 生产环境建议使用 PM2 管理服务。
- 定期备份数据库和项目文件。

---

## ⚠️ 免责声明

本项目仅用于学习、演示和开发测试。任何非法使用风险由使用者自行承担。
